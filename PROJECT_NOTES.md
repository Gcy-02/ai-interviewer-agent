# 项目讲解笔记

这份笔记不是正式文档，主要是面试前自己过一遍用的。

## 我为什么这么做

这个项目想展示的是 Agent 的工作流，而不是做一个很大的面试平台。所以 V1 先把几个关键动作跑通：

```text
简历解析 -> JD 分析 -> 问题生成 -> 回答评分 -> 追问 -> 复盘
```

我没有一上来做数据库、登录和权限。原因很简单：这些当然重要，但它们不是这个项目最想证明的东西。这个版本更像是先证明“Agent 面试流程能跑起来”。

## 我能讲清楚的点

- `LangGraph`：把面试拆成两个阶段，开始面试一张图，提交回答一张图。
- `Tool Calling`：简历解析、JD 分析、评分都拆成工具，不全塞进一个 prompt。
- `Memory`：用 `SESSIONS` 保存当前 session 的问题、回答和评分。
- `Demo Mode`：没有 OpenAI Key 也能跑页面，方便展示。
- `FastAPI`：接口比较直观，`/start_interview` 开始，`/answer` 提交回答，`/report` 复盘。

## 当前边界

- Memory 现在只在内存里，服务重启后会丢。
- 示例 PDF 是英文内容，主要为了 PDF 解析稳定。
- 评分规则是一个能跑的 V1，不是严肃面试评分系统。
- Demo Mode 不是完整大模型效果，只是为了展示流程。

## 面试时可以怎么说

我会说这是一个 V1 项目，重点是把 Agent 的主流程拆清楚。后续如果继续做，我会先加数据库，把 session 和历史问答持久化；然后再把评分维度拆细，比如技术能力、表达能力、项目深度。
